# 模式切换功能实现计划

## 产品定位

### 定位概述

**NovaWay AI Workspace** 是一款面向多角色用户的智能工作空间平台，通过创新的模式切换系统，为工程师、办公人员、创作者、运营人员提供专属的AI协作环境，实现"一人多面，一键切换"的极致体验。

### 一句话亮点

> **NovaWay AI Workspace - 你的全能AI工作舱，五种模式无缝切换，从代码到创意，从办公到运营，一个平台搞定一切。**

### 核心价值主张

| 维度 | 价值描述 |
|------|----------|
| **多角色适配** | 一套工具满足工程师、办公人员、创作者、运营人员的不同需求 |
| **无缝切换** | 一键切换工作模式，无需重新配置环境 |
| **数据隔离** | 各模式独立项目空间，数据安全有保障 |
| **智能协作** | 深度整合AI能力，提升各场景工作效率 |
| **个性化定制** | 支持自定义模式，打造专属工作流 |

### 目标用户

| 用户角色 | 典型需求 | 推荐模式 |
|----------|----------|----------|
| 软件工程师 | 代码编写、调试、项目管理 | 🔧 ForgeMode (锻造模式) |
| 职场白领 | 文档处理、日程管理、协作办公 | 📋 ZenMode (禅意模式) |
| 内容创作者 | AI写作、视频生成、设计创作 | ✨ SparkMode (火花模式) |
| 新媒体运营 | 多平台内容管理、账号运营 | 💓 PulseMode (脉搏模式) |
| 高级用户 | 自定义工作流、个性化配置 | 🚀 FutureMode (未来模式) |

---

## 需求分析

用户需要在桌面端应用中实现一个模式切换系统，包含以下功能：

### 模式定义

| 模式名称  | 名称建议              | 描述                  | 图标建议（都用SVG设计有特色的对应图标） |
| ----- | ----------------- | ------------------- | --------------------- |
| 工程师模式 | ForgeMode (锻造模式)  | 编程专用模式，适用于开发者编写代码   | 🔧 扳手图标，代表工程开发 |
| 办公模式  | ZenMode (禅意模式)    | 适用于普通人办公、日程处理       | 📋 文档图标，代表日常办公 |
| 创作模式  | SparkMode (火花模式)  | AI生成视频、写小说、做短剧、设计海报 | ✨ 火花图标，代表创意激发 |
| 运营模式  | PulseMode (脉搏模式)  | 统一管理小红书、抖音、公众号等平台运营 | 💓 脉搏/波浪图标，代表内容脉动传播 |
| 自定义模式 | FutureMode (未来模式) | 用户自定义配置的模式          | 🚀 火箭图标，代表未来无限可能 |

### 核心功能

1. **模式切换按钮**：位于主页面最左上角，具有特色设计
2. **模式隔离**：不同模式下显示不同的项目列表
3. **设置共享**：设置页面的资源（模型、提供商、agent等）对所有模式通用
4. **数据层级**：模式 → 项目 → 会话 → 对话记录
5. **首页设计**：首次启动显示模式选择首页，便捷切换到各个模式
6. **模式记忆**：切换过模式后记住选择，下次打开默认进入该模式

***

## 技术方案

### 数据模型

```typescript
// 模式类型定义
export type AppMode = 'forge' | 'zen' | 'spark' | 'pulse' | 'future';

export interface ModeConfig {
  id: AppMode;
  name: string;           // 显示名称
  icon: string;           // 图标名称
  description: string;    // 描述
  color: string;          // 主题色
}

export interface UserModeConfig {
  mode: AppMode | null;   // 当前模式（null表示未选择，显示首页）
  hasSelectedMode: boolean; // 是否已选择过模式（用于判断是否显示首页）
  projects: Record<AppMode, string[]>; // 各模式下的项目列表
  customSettings?: Record<string, unknown>;  // 自定义设置（仅future模式）
}
```

### 状态管理

在 `layout.tsx` 中添加模式相关状态管理：

* 当前模式状态（可为null，表示未选择）
* 是否已选择过模式（首次启动判断依据）
* 各模式下的项目列表
* 模式切换逻辑

### 组件设计

1. **ModeSwitchButton** - 模式切换按钮组件
   * 大尺寸、有特色的设计
   * 显示当前模式图标和名称
   * 点击展开模式选择菜单

2. **ModeMenu** - 模式选择菜单
   * 展示所有可用模式
   * 显示模式描述
   * 支持点击切换

3. **ModeHomePage** - 模式选择首页（新增）
   * 首次启动显示
   * 展示5个模式卡片
   * 点击卡片进入对应模式
   * 精美UI设计

### 模式加载机制（核心）

#### 1. 启动加载流程

```
┌─────────────────────────────────────────────────────────────────┐
│                     应用启动                                    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│              读取持久化存储 (persist.ts)                        │
│  - hasSelectedMode: boolean                                   │
│  - currentMode: AppMode | null                                │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
              ┌─────────────────────────────────────┐
              │       hasSelectedMode === false?     │
              └─────────────────────────────────────┘
                    │                    │
                    ▼                    ▼
          ┌──────────────┐    ┌─────────────────────┐
          │ 显示模式选择  │    │ 加载上次选择的模式   │
          │    首页      │    │ 初始化项目列表     │
          └──────────────┘    └─────────────────────┘
```

#### 2. 模式选择流程

```
┌─────────────────────────────────────────────────────────────────┐
│                    用户选择模式 (点击卡片/菜单)                   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│              模式切换处理器 (layout.tsx)                         │
│  1. 更新 currentMode 状态                                       │
│  2. 设置 hasSelectedMode = true                                │
│  3. 持久化到 localStorage                                     │
│  4. 触发项目列表更新                                           │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│              项目列表过滤 (layout.tsx)                           │
│  根据 currentMode 过滤项目：                                     │
│  projects.filter(p => projectModes[p.id] === currentMode)      │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│              界面渲染更新                                        │
│  - 更新模式切换按钮显示                                          │
│  - 更新侧边栏项目列表                                            │
│  - 更新主内容区域                                              │
└─────────────────────────────────────────────────────────────────┘
```

#### 3. 组件组装方式

| 组件 | 职责 | 加载时机 |
|------|------|----------|
| `ModeHomePage` | 模式选择首页 | 首次启动且未选择过模式 |
| `ModeSwitchButton` | 模式切换按钮 | 已选择模式后始终显示 |
| `SidebarShell` | 侧边栏框架 | 已选择模式后显示 |
| `ProjectList` | 项目列表 | 根据当前模式动态加载 |
| `SessionPanel` | 会话面板 | 选择项目后显示 |

#### 4. 项目数据隔离机制

```typescript
// 项目数据结构
interface Project {
  id: string;
  name: string;
  worktree: string;
  mode: AppMode;  // 新增：项目所属模式
  // ... 其他字段
}

// 项目列表过滤逻辑
const filteredProjects = createMemo(() => {
  const currentMode = layout.mode.current()
  if (!currentMode) return []
  return layout.projects.list().filter(p => p.mode === currentMode)
})
```

#### 5. 设置页面共享机制

设置页面的资源（模型、提供商、agent等）通过独立的全局状态管理，不依赖于当前模式：

```
┌─────────────────────────────────────────────────────────────────┐
│                    全局设置状态 (settings.tsx)                   │
│  - models: Model[]          ← 所有模式共享                      │
│  - providers: Provider[]    ← 所有模式共享                      │
│  - agents: Agent[]          ← 所有模式共享                      │
│  - preferences: Preferences ← 所有模式共享                      │
└─────────────────────────────────────────────────────────────────┘
```

***

## 文件修改清单

| 文件路径                                              | 修改内容        | 类型 |
| ------------------------------------------------- | ----------- | -- |
| `packages/app/src/context/layout.tsx`             | 添加模式状态管理    | 修改 |
| `packages/app/src/pages/layout/sidebar-shell.tsx` | 添加模式切换按钮    | 修改 |
| `packages/app/src/components/mode-switch.tsx`     | 新建模式切换组件    | 新建 |
| `packages/app/src/components/mode-home.tsx`       | 新建模式选择首页    | 新建 |
| `packages/app/src/utils/persist.ts`               | 添加模式持久化支持   | 修改 |
| `packages/app/src/pages/layout.tsx`               | 根据模式过滤项目    | 修改 |
| `packages/app/src/pages/home.tsx`                 | 修改首页逻辑      | 修改 |
| `packages/app/src/i18n/zh.ts`                     | 添加模式相关国际化文案 | 修改 |

***

## 实现步骤

### 步骤 1：定义模式类型和配置

在 `packages/app/src/context/layout.tsx` 中添加：

* 模式类型定义（5个模式）
* 模式配置常量
* 当前模式状态（支持null）
* 模式切换方法
* 是否已选择过模式的状态

### 步骤 2：创建模式切换组件

创建 `packages/app/src/components/mode-switch.tsx`：

* ModeSwitchButton 组件
* ModeMenu 下拉菜单组件
* 动画效果

### 步骤 3：创建模式选择首页

创建 `packages/app/src/components/mode-home.tsx`：

* 5个模式卡片展示
* 精美的渐变背景和动画效果
* 点击卡片切换到对应模式

### 步骤 4：集成到侧边栏

修改 `packages/app/src/pages/layout/sidebar-shell.tsx`：

* 在侧边栏顶部添加模式切换按钮
* 调整布局结构

### 步骤 5：实现首页逻辑

修改 `packages/app/src/pages/home.tsx`：

* 首次启动（未选择过模式）显示模式选择首页
* 已选择过模式直接进入对应模式的项目列表

### 步骤 6：实现项目隔离

修改 `packages/app/src/pages/layout.tsx`：

* 根据当前模式过滤项目列表
* 确保设置页面资源共享

### 步骤 7：添加国际化

在 `packages/app/src/i18n/zh.ts` 中添加模式相关翻译：

* 5个模式名称
* 5个模式描述
* 首页文案

### 步骤 8：持久化支持

修改 `packages/app/src/utils/persist.ts`：

* 添加模式状态持久化（当前模式、是否已选择过模式）
* 保存用户选择的模式
* 下次启动自动恢复

***

## 依赖关系

```
layout.tsx (状态管理)
    ↓
home.tsx (首页逻辑)
    ↓
mode-home.tsx (首页组件)
    ↓
sidebar-shell.tsx (UI集成)
    ↓
mode-switch.tsx (组件实现)
    ↓
i18n/zh.ts (国际化)
    ↓
persist.ts (持久化)
```

***

## 风险评估

| 风险   | 描述            | 应对方案               |
| ---- | ------------- | ------------------ |
| 首次启动 | 首次启动需要显示首页 | 添加 `hasSelectedMode` 状态，首次为false显示首页 |
| 数据迁移 | 现有项目需要关联到模式 | 首次选择模式时，提示用户将现有项目迁移到所选模式 |
| 性能影响 | 模式切换时重新加载项目列表 | 使用缓存机制优化加载速度 |
| 向后兼容 | 旧版本数据没有模式概念 | 检测到无模式数据时，标记为未选择过模式，显示首页 |

***

## 测试计划

1. **单元测试**：模式切换逻辑测试
2. **集成测试**：模式切换与项目列表的交互
3. **UI测试**：模式切换按钮和首页的显示和交互
4. **持久化测试**：模式状态保存和恢复
5. **首次启动测试**：首次启动显示首页，选择模式后下次启动进入对应模式

***

## 设计规范

### 视觉设计

* **模式切换按钮**：大尺寸圆形按钮，位于侧边栏顶部，带有渐变背景和发光效果

* **图标**：使用项目现有图标库，为每个模式设计独特图标

* **颜色**：每个模式有独特的主题色
  - ForgeMode（锻造）：钢铁蓝 #4A90D9
  - ZenMode（禅意）：竹林绿 #4CAF50
  - SparkMode（火花）：火焰橙 #FF9800
  - PulseMode（脉搏）：珊瑚红 #FF6B6B
  - FutureMode（未来）：星空紫 #9C27B0

* **动画**：切换时有平滑过渡效果，卡片悬浮有缩放动画

* **首页设计**：
  - 全屏渐变背景，从深蓝到紫色
  - 5个模式卡片呈放射状排列
  - 卡片带有玻璃拟态效果
  - 悬浮时显示模式描述

### 交互设计

* **首页交互**：
  - 首次启动显示模式选择首页
  - 点击模式卡片进入对应模式
  - 卡片悬浮显示详细描述
  - 切换时有缩放过渡动画

* **模式切换交互**：
  - 点击侧边栏按钮展开下拉菜单
  - 悬停显示模式描述
  - 选择模式后平滑切换
  - 当前模式有明显高亮标识

* **记忆功能**：
  - 选择模式后自动保存
  - 下次启动直接进入该模式
  - 设置页面可清除模式记忆（恢复首次启动状态）

***

## 进度预估

| 阶段      | 预估时间 |
| ------- | ---- |
| 需求分析    | 1天   |
| 设计与规划   | 1天   |
| 核心功能开发  | 4天   |
| UI集成与测试 | 2天   |
| 文档与优化   | 1天   |

***

## 备注

1. 模式切换按钮设计要有特色，可以考虑使用渐变背景、发光效果等
2. 各模式的项目数据相互独立，但设置页面通用
3. 未来模式支持用户自定义配置，预留扩展接口
4. PulseMode（脉搏模式）专为内容运营设计，支持小红书、抖音、公众号等多平台管理场景
5. 首次启动显示精美首页，方便用户选择适合自己的模式
6. 模式选择会被记忆，下次启动自动进入上次选择的模式
7. 项目通过 `mode` 字段关联到特定模式，切换模式时通过过滤显示对应项目