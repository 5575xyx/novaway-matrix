 \"pending\" | \"applied\" | \"dismissed\"\nexport type CandidateSource = \"all\" | \"background\" | \"session-end\"\nexport type ValidationStatus = \"pending\" | \"validated\" | \"failed\"\nexport type EvolutionDomain = \"general\" | \"coding\" | \"office\" | \"personal\" | \"research\" | \"ops\"\nexport type DomainFilter = \"all\" | EvolutionDomain\n\nexport const evolutionDomainFilters: DomainFilter[] = [\n  \"all\",\n  \"general\",\n  \"coding\",\n  \"office\",\n  \"personal\",\n  \"research\",\n  \"ops\",\n]\n'''\n    )\n    t = t.replace(\n'''export function evolutionValidationLabel(status: string | undefined | null) {\n  switch (status) {\n    case \"validated\":\n      return \"已验证\"\n    case \"failed\":\n      return \"验证失败\"\n    default:\n      return \"待验证\"\n  }\n}\n''',\n'''export function evolutionValidationLabel(status: string | undefined | null) {\n  switch (status) {\n    case \"validated\":\n      return \"已验证\"\n    case \"failed\":\n      return \"验证失败\"\n    default:\n      return \"待验证\"\n  }\n}\n\nexport function filterEvolutionByDomain<T extends { domain?: string | null }>(\n  items: readonly T[] | undefined | null,\n  domain: DomainFilter,\n) {\n  const list = Array.isArray(items) ? items : []\n  if (domain === \"all\") return list.slice()\n  return list.filter((item) => (item.domain ?? \"general\") === domain)\n}\n\nexport function evolutionDomainCounts(items: readonly { domain?: string | null }[] | undefined | null) {\n  const list = Array.isArray(items) ? items : []\n  const counts: Record<DomainFilter, number> = {\n    all: list.length,\n    general: 0,\n    coding: 0,\n    office: 0,\n    personal: 0,\n    research: 0,\n    ops: 0,\n  }\n  for (const item of list) {\n    const domain = (item.domain ?? \"general\") as EvolutionDomain\n    if (domain in counts) counts[domain] += 1\n  }\n  return counts\n}\n\nexport function evolutionDomainFilterLabel(domain: DomainFilter) {\n  if (domain === \"all\") return \"全部领域\"\n  return evolutionDomainLabel(domain)\n}\n'''\n    )\n    p.write_text(t, encoding=\"utf-8\")\n    print(\"evolution helpers domain filter added\")\n'@ | python -","workdir":"E:\\AImoney\\NovaWay-Matrix\\novaway-coder"}
```


## 2026-07-25T05:25:02.267Z shell_command

```json
{"command":"@'\nfrom pathlib import Path\nimport re\n\n# settings-memory.tsx\np = Path(r\"packages/app/src/components/settings-memory.tsx\")\nt = p.read_text(encoding=\"utf-8\")\n# expand imports\nt = t.replace(\n'''  memoryConfidenceLabel,\n  memoryDomainLabel,\n  memoryOperationLabel,\n  memoryReviewCounts,\n  memoryReviewSourceCounts,\n  memoryVersionLabel,\n  type CandidateSource,\n  type ReviewStatus,\n} from \"./settings-memory.helpers\"\n''',\n'''  filterMemoryByDomain,\n  memoryConfidenceLabel,\n  memoryDomainCounts,\n  memoryDomainFilterLabel,\n  memoryDomainFilters,\n  memoryDomainLabel,\n  memoryOperationLabel,\n  memoryReviewCounts,\n  memoryReviewSourceCounts,\n  memoryVersionLabel,\n  type CandidateSource,\n  type DomainFilter,\n  type ReviewStatus,\n} from \"./settings-memory.helpers\"\n'''\n)\nif \"domainFilter\" not in t:\n    t = t.replace(\n        'const [scopeFilter, setScopeFilter] = createSignal<\"all\" | \"global\" | \"project\">(\"all\")',\n        'const [scopeFilter, setScopeFilter] = createSignal<\"all\" | \"global\" | \"project\">(\"all\")\\n  const [domainFilter, setDomainFilter] = createSignal<DomainFilter>(\"all\")',\n    )\n# update filteredCandidates\nold_fc = '''  const filteredCandidates = createMemo(() => {\n    const base = filterMemoryReviewCandidates(candidates.data ?? [], source(), modeGroup())\n    if (scopeFilter() === \"all\") return base\n    return base.filter((item) => item.scope === scopeFilter())\n  })\n'''\n# try find actual\nif old_fc not in t:\n    m = re.search(r'const filteredCandidates = createMemo\\(\\(\\) => \\{[\\s\\S]*?\\n  \\}\\)', t)\n    print('filteredCandidates block:')\n    print(m.group(0) if m else 'NOT FOUND')\nelse:\n    print('exact filteredCandidates found')\n'@ | python -","workdir":"E:\\AImoney\\NovaWay-Matrix\\novaway-coder"}
```


## 2026-07-25T05:25:31.342Z shell_command

```json
{"command":"@'\nfrom pathlib import Path\nimport re\n\np = Path(r\"packages/app/src/components/settings-memory.tsx\")\nt = p.read_text(encoding=\"utf-8\")\n\nt = t.replace(\n'''  const filteredCandidates = createMemo(() => {\n    const base = filterMemoryReviewCandidates(candidates.data ?? [], source(), modeGroup())\n    if (scopeFilter() === \"all\") return base\n    return base.filter((item) => item.scope === scopeFilter())\n  })\n''',\n'''  const filteredCandidates = createMemo(() => {\n    const base = filterMemoryReviewCandidates(candidates.data ?? [], source(), modeGroup())\n    const scoped = scopeFilter() === \"all\" ? base : base.filter((item) => item.scope === scopeFilter())\n    return filterMemoryByDomain(scoped, domainFilter())\n  })\n'''\n)\n\n# filtered entries\nold_fe = '''  const filteredEntries = createMemo(() => {\n    const